import styled from "styled-components";

const Container = styled.div`
  .container-carousel {
    max-height: 400px;
    .content-banner {
      width: 100%;
      max-height: 400px;
      object-fit: cover;
      background: #364d79;
    }
  }
`;

export default Container;
